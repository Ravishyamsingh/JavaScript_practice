// Declare a variable using var (function-scoped)
var age = 12;

// Declare a variable using let (block-scoped)
let num = 13;

// Declare a constant value (cannot be reassigned)
const gender = 12;

// Display all variables together in a table format in the console
console.table([age, num, gender]);
